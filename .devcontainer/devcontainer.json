{
    "name": "Python",
    "image": "mcr.microsoft.com/devcontainers/python:latest",
    "postCreateCommand": "curl -LsSf https://astral.sh/uv/install.sh | sh && sudo apt update && sudo apt install -y libhdf5-serial-dev"
}